spec:
  name: trophy
  services:
  - name: leaderboards
    image:
      registry_type: DOCKER_HUB
      registry: andtechstudios
      repository: trophy
    envs:
    - key: AUTH_KEY
      value: secret
      scope: RUN_AND_BUILD_TIME
