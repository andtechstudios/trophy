spec:
  name: trophy
  services:
  - envs:
    - key: AUTH_KEY
      scope: RUN_AND_BUILD_TIME
      type: SECRET
      value: secret
    image:
      registry: andtechstudios
      registry_type: DOCKER_HUB
      repository: trophy
    name: andtechstudios-trophy