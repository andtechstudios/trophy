services:
  web:
    image: ghcr.io/andtechstudios/trophy
    volumes:
      - trophy:/app
    ports:
      - 5000:5000
    restart: always
    environment:
      AUTH_TOKEN: secret

volumes:
  trophy:
